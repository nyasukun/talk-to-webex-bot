# 検証記録

実施日: 2026-09-09。公開用に一般的な条件だけを記録しています。個人設定・宛先・認証情報・実録音は含みません。

## 確認済み

- M4 Pro・24GB・macOS 26.6.2、Swift 6.3.3のCommand Line Toolsでdebug / releaseビルド成功。
- .app作成、Info.plist配置、アドホック署名と厳密な署名検証成功。
- 再ビルドで画面収録の旧登録が残るという実機報告を受け、ローカル証明書を再利用するビルドへ変更。キーチェーンへの証明書保存、異なるバイナリの署名条件一致、既存証明書の再利用、修正版.appへの署名と検証が完了。その後、画面収録を再許可した後の取得と、再ビルド後の権限保持をユーザ操作で確認。
- アプリの表示名・ウィンドウ名・生成する.app名を「Talk to Webex bot」へ変更し、releaseビルド成功。Bundle IDと既存設定の保存先は維持。ユーザ操作による署名後、名前変更版の厳密な署名検証、以前の証明書署名との識別条件一致、新しいパスからの起動を確認。
- Swiftの主要ロジック・ネイティブ処理・キーチェーン操作・サポート機能58件、Pythonのworkerロジック16件が成功（ビルド8）。
- 初回取得スクリプトで固定Python依存とWhisper turbo / large-v3 / Qwen3-TTSをアプリ用フォルダへ配置。
- ネットワーク禁止sandboxでVAD診断成功。
- 同sandboxでWhisper2種が3つの合成日本語文を認識。無音、白色雑音、440Hz単音を拒否。
- 同一合成音声による話者埋め込み比較成功。
- 一般的な日本語文字列を描いた画像にVision OCRを適用して認識成功。PCM WAVの長さ・形式・ファイル権限の検証成功。
- 同sandboxでQwen3-TTSの参照音声つき生成成功。有限値を持つWAVで5秒以上の音声を確認し、Whisperで再認識できた。
- ユーザによるアプリ内のトークン登録。UIで有効状態とページネーションを含むDM取得を確認。
- ユーザ操作で、連続待受 → 合言葉と指示の認識 → 送信前確認 → Webex実送信 → 最終返信の一度だけの読み上げを確認。
- 旧署名の画面収録登録を対象アプリだけリセットし、ユーザによる再許可と再起動後に「許可済み」表示を確認。
- ユーザ操作で、合言葉 → 一呼吸 → 画面への指示 → 正しいウィンドウ画像・OCRの送信前確認 → Webex実送信 → 返信読み上げを確認。
- 実返信の診断表示で「取得15回・返信候補1件・途中表示1件・同一IDの本文更新1回・読み上げ完了」をユーザが確認。実際の同一ID更新を観測した後の読み上げを確認。
- ビルド番号を更新して同じ証明書で再ビルド・再署名。署名条件の一致を検証し、ユーザ操作で追加の承認なしに画像・OCRの確認画面が出ることを確認。
- ユーザがアプリ内で本人の参照音声を録音し、Qwen3-TTSで新しい文を生成・試聴。「自分の声に似ている」と本人が確認。追加学習は行っていない。
- 短いVAD区間の個別照合で本人を誤拒否する事象を診断し、音声区間を連結して十分な長さの窓で比較するよう修正。本人の別録音で、しきい値を下げずに通過することをオフライン確認。短い末尾窓を作らないこと、不一致区間や非有限値を拒否することをテスト。
- 修正後、本人照合ONで合言葉と指示を区切り、Webexへ実送信し、本人参照音声を使ったQwen3-TTSで実返信を読み上げる一連の操作に成功。ユーザが読み上げによる自己反応なしと待受復帰を確認。
- 修正後のオフライン検証で、一般的な別の合成音声を本人参照と照合して拒否することを確認。
- 初回E2Eで入力バーが動かない事象を受け、Mac追加音声処理を任意・初期OFFに変更。送信しない5秒テストでマイク・文字起こし・合言葉一致を確認し、続けて連続待受からの送信を確認。
- workerと同じOS sandboxがネットワークsocket/connectを拒否することを確認。

主要ロジックには、テンプレートの条件範囲、合言葉除去・時間切れ、DM順序、トークン発行時刻不明、APIホスト制限、401、429、成否不明POSTの再送禁止、正確なmultipart、途中表示・同一ID更新・古い返信・別スレッド・重複本文の除外が含まれます。

- ビルド5を既存の証明書で署名して同じ.appパスへ配置。厳密な署名検証、過去の証明書署名との識別条件一致、最新workerの同梱を確認。
- 起動時の非対話キーチェーン読み出し、明示操作でのトークン読み込み、返信待ちのソナー音とQwenの先行準備・逐次再生を実装。音声片の順序・停止・失敗時の破棄、ソナーの無音区間と音量範囲を自動テスト。
- 声の判定の初期値を利便性重視の優先モードへ変更。短い発話・単独発話・区別が不確かな発話を保持し、明確に異なる声が混在した場合だけ登録した声を優先する判定をテスト。
- 旧ビルド5のネットワーク禁止下のQwen逐次生成で、初回音声片4.220秒、準備後0.218秒。8.32秒の合成音声を12片で生成、順序どおり結合。別の新規workerでは先行準備3.727秒、その後の最初の音声片0.227秒。結合した音声をWhisperで再認識し、文頭・後半の内容を確認。停止時の残り音声破棄を確認。
- 優先モードを2種類のMac標準合成音声で実行。短い「短く答えて」の認識、単独発話の受付、間を置いて交互に話す音声から参照側の予定の指示を残し、別の声の散歩の発話を除くことを確認。実際の人間による複数話者の評価は未実施。

- ビルド5の手動アクセス操作後、Webex認証が有効になることをユーザが確認。一方で複数回のパスワード確認を報告。読み込み前に既存アクセスリストを更新していた処理を除去し、読み込みは1回、通常の更新は値だけに修正。読み込み・キャンセル・非対話フラグ復元・値更新・初回追加を、実トークンに触れない差し替えた処理でテスト。
- DM一覧を直近5件、フィルタ時は一致する直近5件までの取得に変更。ページをまたぐ検索、重複除外、5件に達した後の取得停止、5件未満、キャンセル済み検索の追加取得停止をテスト。

- 返信監視間隔をms表記・初期値100msへ変更。旧初期値のみの移行、任意の設定値の保持をテスト。読み上げ用の日本語・カタカナ・短文改行指示をテンプレートに追加し、既存の編集内容を残す一度だけの移行をテスト。
- 改行ごとに音声を生成し、句点による補助分割と60文字上限を追加。異常に長い生成はその文の再生前に停止。PCMの分割で全サンプルとチャンネルを保持することをテスト。
- ネットワーク禁止下で、一般的な合成参照による8文を2回生成。先行準備3.486秒、最初の文0.673秒・0.702秒、全体生成5.423秒・5.465秒、音声21.84秒・23.20秒。全16文をWhisperで再認識し、後半を含めた内容を確認。ピークMetalメモリ4.004 GiB。実際の本人の参照音声や聴感の検証ではない。
- 文ごとの生成に変更した後も、実モデルの生成完了・再利用・途中停止による残り音声の破棄を、ネットワーク禁止下で確認。ビルド6のreleaseコンパイルと48件の自動テストが成功。同じ証明書で署名して.appを入れ替え、厳密な署名検証・以前との署名条件一致・最新workerの同梱・正しいパスからの起動を確認。最新変更の実機E2Eは未実施。

## ビルド7の追加検証

- 最終候補のdebug / releaseビルドが成功し、Swift48件とPython13件の自動テストが成功。ユーザによる終了後に同じ証明書で署名して.appを入れ替え、厳密な署名検証・以前との署名条件一致・ビルド7・最新workerの同梱・正しいパスからの起動を確認。UI取得ツールで実ウィンドウを読み取れなかったため、実表示と操作の確認はユーザ操作で行う。
- 二桁以上・全角の数字を意味に合ったひらがなへ変換する指示を追加。既存テンプレートを保持し、一度削除した指示を再挿入しない移行をテスト。
- 2種類の合言葉、続けて話す場合と一呼吸置く場合の送信モード保持、曖昧な合言葉の拒否、DM変更時の返信先保護をテスト。スレッド送信の本文とparentIdを差し替えた通信で検証し、成否不明時に再送しないことを確認。
- 同じスレッド内の新しい返信と同一ID更新を追跡し、別スレッド・過去のID・スレッド外の返信を除くことをテスト。実際の相手への追加返信は未確認。
- 認証の更新画面、公式ページを一度だけ開く制御、ブラウザ起動失敗からの回復、正常な定期確認が手動入力画面を閉じないことを、実認証・実ブラウザを使わずにテスト。
- 固定イベントと数値だけの診断ログについて、500件の上限、保存と再読み込み、0600の権限、エラー本文を記録しないことを確認。メニューバーの通常色と赤・緑の画像特性も確認。
- 音声形式の変更通知と4秒間のフレーム途絶を監視し、録音エンジンを再作成する処理を実装。30秒に2回までという再接続制限をテスト。5秒マイクテストには録音秒数・入力最大値・音声形式のログと、フレーム未到達／音量ゼロを区別する案内を追加。
- `scripts/render-ui.sh` により、一般的な架空データで設定・ログ・認証画面と明暗のホームをネットワーク禁止下で描画し、内容と配置を確認。サイドバーはウィンドウ全体の画像化で欠落したため単独で描画して確認。実ウィンドウの選択操作・合成表示は別途確認する。
- リポジトリの `.agents/skills/talk-to-webex-bot-support` に導入・操作・トラブル対応用スキルを追加し、スキル形式の検証が成功。公開対象62ファイルの禁止語・認証情報・個人データ検査が成功。

## ビルド8の追加検証

- debug / releaseコンパイル成功。Swift58件とPython16件、計74件のテストに成功。ユーザによる終了後、既存と同じ証明書で署名して同じ.appパスへ入れ替え。厳密な署名検証、以前との署名条件一致、ビルド8、最新workerの同梱、正しいパスから1プロセスの起動を確認。UI取得ツールはウィンドウを取得できなかったため、実表示・操作はユーザ確認が必要。
- 25秒の区間超過を8回続けても常時待受が続くこと、認識待ちの混雑で待受を停止しないこと、確認・送信・読み上げ中の音声が状態を上書きしないことを検証。古い認識結果の失効処理も実装。実マイクでの長時間動作は別途確認する。
- 待受中だけ電源アクティビティを保持し、停止時に解除する制御を差し替えたAPIで検証。画面スリープを禁止しないこと、設定変更時の再取得、二重取得防止を確認。画面ロック中の実録音は未確認。
- スクショ無効時に取得処理を呼ばないこと、対象不在・前面変更・ロック時に画像とOCRブロックを省くこと、権限不足とキャンセルは別に扱うことを検証。ロック状態は合成したセッション情報による検証で、OS上の実ロックではない。
- スレッド返信は新しい文字起こしと文脈継続・読み上げの指示だけを本文にし、前のボット回答を引用しない。明示的なparentIdを含む送信データ、旧初期値の一度だけの移行、独自テンプレートと移行後の編集保持をテスト。
- キーチェーンの自動・手動読み出し時の対話フラグと異なる失敗案内を、実保存項目を使わずに検証。自動読み出しで-25293、画面が出ないという報告に対して、実行中ビルド7のコード署名が有効であることを確認。改修後の手動操作とmacOSの確認画面は未確認。
- 未保存設定の取り消し、DM選択が他の設定を勝手に保存しないこと、初期準備の次のステップをテスト。録音終了以外の設定を録音中に変更できないように修正。
- 読み上げを改行・句読点・記号で最大32文字に事前分割。長さ上限に達した未再生区間の再分割、文字の順序と末尾の保持、不正な音声を再試行しないこと、再分割回数の上限をテスト。
- ネットワーク禁止下、一般的な合成参照音声でQwen3-TTSを実行。短文8区間を2回、句読点・記号を含む長い段落を6区間、上限を意図的に発生させた再分割テストを3区間で生成し、計25区間の有限な音声をWhisperで再認識。長い段落は22.40秒の音声、生成6.152秒、最初の区間0.578秒。上限テストは1回の再分割後も末尾まで生成。ピークMetalメモリ4.004 GiB。
- 再認識には「午前中」が別の語になる等の表記差がある。声質・発音の完全な正確さを保証する評価ではない。その後、ユーザがビルド8のQwen3-TTS試聴で本人の参照音声を使い、句読点・記号を含む長い段落と末尾の文を確認。途中停止や同じ言葉の繰り返しなく最後まで読めたと報告。Webexへの送信は伴わない試聴であり、実返信全般の評価とは区別する。
- 880×650と1080×780の設定、ホーム、ログ・診断、画面省略・スレッド返信の確認画面を一般的なデータで描画し、主要な内容と操作領域を確認。[UI/UXレビュー](ux-review.md)に根拠と変更理由を記録。実サイドバーの合成表示・キーボード・VoiceOverは別途確認する。
- リポジトリ専用スキルを現行の返信テンプレート・常時待受・音声分割仕様に更新。公開対象65ファイルの禁止語・認証情報パターン・個人データ形式の検査が成功。

## 単語途中の音声分割の修正

- ビルド8の途切れ・読み飛ばし・反復の報告を受け、32文字でカタカナ語を切り、丁寧語の末尾だけを別生成する分割を再現。句読点が文頭に近い場合も区切りとして使い、記号がない部分はmacOS内蔵の単語境界を使うよう修正した。短い文末を残さず、接頭辞と単語、小書きの仮名・結合文字の分離を避ける判定を追加した。
- Swift58件・Python20件が成功し、releaseコンパイルも成功。通常の実行制限下ではVision OCRテストが環境エラーになったため、制限外で標準テスト一式を再実行して成功を確認した。実送信・実トークンの読み出しは行っていない。
- 登録済みの本人参照を使い、報告された本文をネットワーク禁止下でローカル生成した。6区間、計19.68秒の有限な音声を10.49秒（準備を含む）で生成し、再分割は0回。Whisperによる再認識で文頭・文末と各区間の内容を確認した。時刻の再認識には数字の区切りの差が残り、発音の正確さと実際の再生中の途切れ・反復は本人の試聴で確認が必要。本文・参照音声・生成音声をリポジトリへ保存せず、一時生成音声は検証後に削除した。
- ユーザによる終了後、ビルド9を同じ証明書で署名して既存.appへ反映。厳密な署名検証、以前との署名条件一致、ビルド番号と修正済みworkerの同梱を確認し、正しい.appパスからの起動をプロセスで確認した。UI取得は失敗したため、画面表示と本人による試聴は未確認。

## ビルド10：改行ごとの生成と先行再生

- ビルド9の長文読み上げで3行目から4行目の間が詰まる報告を受け、32文字への行内分割と、再生残り3秒まで次の生成を待つ処理を変更した。空行を除く1行を独立した生成単位にし、160文字を超える行だけ事前分割する。上限による再生成も行内で完了させ、断片を結合してから再生する。
- 最初の2行を準備して再生を開始し、モデルの逐次生成と音声エンジンの再生を並行させた。先行音声は最大3区間に制限し、順序・単一行・空入力・生成失敗・停止時の破棄・生成が遅い行を含む先行再生をテストした。長い再生を総経過30秒で誤停止せず、再生が30秒進まない場合だけ停止することもテストした。
- Swift65件・Python22件、計87件が成功。実際のAVAudioEngineをオフラインで動かし、最初の2行の準備中は無音であること、再生中に次の行を追加して全PCMサンプルが欠落なく順に出力されること、完了通知で再生待ちの数値がゼロに戻ることを確認した。オフラインではデバイス再生完了通知が利用できないため、描画完了通知を使って検証し、通常の再生はデバイス再生完了通知を使う。
- 登録済みの本人参照と報告された15行をネットワーク禁止下で生成した。15行すべて1回ずつの独立生成で成功し、再分割は0回。合計132.48秒の音声を31.81秒で生成、準備済みモデルから最初の2行がそろうまで1.53秒。生成時間と音声長に基づく先行再生の計算では生成待ち0回。3・4・15行目をWhisperで再認識し、時刻を含む内容と末尾を確認した。生成時点のMLXピークMetalメモリは3.266 GiB。実際の再生で待ちが皆無であることや本人の聴感を保証する測定ではない。本文と音声はリポジトリに保存せず、生成音声は一時領域から削除した。
- 一般的な合成参照による既存のモデル検証も成功。8行を2回、長い1行を1回、上限を意図的に発生させる2行を1回生成した。長い1行は23.20秒の音声を5.39秒で生成し、上限テストでは1回の再分割後も元の2行として結合・再認識できた。
- 行番号・総行数・生成秒・音声秒・再生残秒と、先行音声が尽きたイベントを診断ログへ追加。会話本文・参照本文・音声はログに含めない。
- releaseコンパイルと設定画面の描画が成功し、880×650の試聴欄で更新した説明文と操作ボタンが収まることを確認した。対象アプリの終了を確認してビルド10を既存証明書で署名・反映し、厳密な署名検証、以前との署名条件一致、最新workerの同梱、正しい.appパスからの起動を確認した。UI取得は失敗したため、実表示と本人による同じ文章の試聴は未確認。
- 公開対象監査では、既存のartifactsディレクトリにある別作業の動画などが検出された。そのディレクトリを除くコード・文書には検出なし。動画や別作業のファイルは変更せず、リポジトリ全体の公開監査成功とは扱っていない。

## ビルド11：最初の音声を待たせない変更

- ビルド10の診断値では、準備済みモデルでも返信確定から再生開始まで26.027秒だった。入力の総行数は1行と記録され、最初の2区間に15.380秒・10.573秒を要し、合計3回の上限再分割も発生していた。本文が長い1行として届いたことと、2区間を先に用意する処理が開始を遅くしていた。本文自体やWebexの生レスポンスは取得していない。
- 最初の音声を受け取った直後に再生し、2区間目の生成開始より前に再生が始まるよう変更。冒頭だけ短い文または自然な境界で32文字以内に区切る。160文字を超える行は文末で先に分割する。通常の後続行と、上限時の未再生断片の結合は維持する。Markdownに改行が多く、装飾を外した内容が通常本文と空白以外同じ場合は改行を復元する。
- Swift67件・Python25件、計92件が成功。最初の区間だけでの開始、開始後の生成、停止・失敗時の破棄、先行音声の上限、実AVAudioEngineのPCM順序、返信の確定待ちと重複排除を確認。改行が消えた段落、冒頭だけの短縮、後続行の維持、内容の異なるMarkdownを採用しないことを回帰テストへ追加した。
- 登録済みの参照音声と一般的な15行の検証文をネットワーク禁止下で生成。改行をなくした場合、最初の音声は準備済みモデルから0.519秒、20区間の合計109.44秒の音声を32.123秒で生成した。再分割は1回、生成時間からの計算では途中待ち0回。MLXピークMetalメモリは3.183 GiB。改行ありの場合は15区間、冒頭0.322秒、合計123.68秒の音声を29.420秒で生成し、計算上は2行目の前に0.258秒の待ちがあった。音声の実再生を測った値ではなく、モデル読み込み・返信確定待ち・音声装置の遅延を含まない。
- 改行ありの1・3・4・15行目をWhisperで再認識した。冒頭と3行目は内容を確認できたが、4行目には反復、15行目には一部の読み違いを示す認識結果があった。モデル自体の誤発音・反復の解消や本人の聴感は未確認。入力・参照本文・音声はリポジトリへ追加せず、一時生成音声は削除した。
- releaseコンパイルと設定画面の描画が成功し、880×650でも説明文と操作が収まることを確認した。公開対象監査の検出は別作業のartifactsディレクトリ内だけだった。対象アプリの終了を確認し、ビルド11を既存証明書で署名して反映した。厳密な署名検証、以前との署名条件一致、最新workerの同梱、更新した.appの実行パスからの起動を確認した。本人による開始時間と連続性の確認は未完了。

## ビルド12：参照録音から引き継がれる背景ノイズの軽減

- 指定された10行を6条件・各2回生成し、波形と自動音声認識で検証した。最終実装では静かな部分の中央値が−52.9から−62.8 dBFS、文中の間が−51.7から−59.8 dBFSとなった。準備済みモデルからの最初の音声生成は0.85秒と0.56秒。本人による聴感評価ではなく、固有名詞の読みなどは未解決。[調査・比較条件](speech-quality.md)に測定方法とSarashinaの採用条件を記録した。
- 参照録音をメモリ上で軽くノイズ低減し、録音更新・サンプルレート・ON/OFFでキャッシュを更新する。元録音への書き込みは行わない。Qwenの設定に「参照音声の背景ノイズを軽減」を追加し、初期ONで既存設定を移行する。明示したOFFは保存後も維持する。
- Swift68件・Python28件、計96件が成功。ノイズ低減と音声の長さ・発声タイミングの維持、ノイズ推定できない場合の処理省略、ステレオ・サンプルレート変換、キャッシュ更新、元録音の保持、設定の移行とOFFの保持を検証した。既存のPCM順序・停止・先行生成・返信確定・重複防止も回帰テスト成功。
- releaseビルド、Qwen設定画面の880×650プレビュー、既存証明書での署名、厳密な署名検証、以前との署名条件一致、最新workerの同梱を確認した。対象アプリを通常終了してビルド12へ反映した。本人によるノイズと声の響きの確認は未完了。公開対象監査では別作業のartifactsディレクトリ外に検出はなかった。

## 未完了の実機確認

- Macのスピーカーから流れる会議相手の声と本人の発話が重なると受け付けられないという実機報告。現行の話者優先は同時音声を分離しない。Mac追加音声処理をONにして待受を開始した際、会議のスピーカー音量が下がることをユーザが報告。音量を維持したいという要件に合わせ、該当アプリを終了し、ローカル設定の追加音声処理だけをOFFへ戻して再起動した。再起動とOFFの保持を確認。回り込みの抑制効果は確認できておらず、再生音量を変えない方法の検討が残る。周波数フィルタだけによる話者抽出は採用していない。
- ビルド8の実マイクで25秒の背景音後も待受を継続すること、画面ロック中の待受・画像省略・送信前確認。
- ビルド8の設定画面・ログ・認証更新ポップアップ、数字のひらがな指定に従った実返信、追加返信の実スレッド動作、メニューバーの実際の色の遷移。
- 会議中に内蔵マイクの入力バーが動かなかったという報告への修正。形式変更通知・継続的なフレーム途絶監視と回数制限付き再接続を実装したが、報告された現象の原因は未特定。会議中の5秒テストと、会議開始・終了時の待受を確認する。

- 修正版のプロンプトに沿った実返信、本人の声での実返信の発音・反復・途切れ（試聴用の長い段落は確認済み）、100ms設定の実APIでの動作、キーチェーン読み込み・再起動と、実APIでの5件制限・フィルタ検索、ソナー音の聴感、本人参照音声による逐次再生の開始時間と連続性、短い指示と実際の複数話者の優先。

- 実際のマイクでの音楽・雑音・別話者への誤反応率。本人照合と本人音声での実返信、読み上げによる自己再起動の抑制は今回のE2Eで確認済み。
- Intel Mac、他のmacOSバージョン、異なるマイク、長時間常駐の安定性。
- 配布用署名・公証。ローカル証明書での署名は配布用署名の代わりにはならない。

これらを完了扱いにせず、[E2E手順](e2e.md)で確認してから検証記録を更新します。合成音声3文の結果から一般的な日本語認識精度や本人の声質を保証しません。

## 再実行

```bash
scripts/test.sh
scripts/build.sh
scripts/render-ui.sh
scripts/smoke-local.sh --voice
scripts/smoke-local.sh --model whisper-large
/usr/bin/sandbox-exec -p '(version 1) (allow default) (deny network*)' \
  "$HOME/Library/Application Support/LocalVoiceRelay/runtime/.venv/bin/python" scripts/smoke-streaming.py
/usr/bin/sandbox-exec -p '(version 1) (allow default) (deny network*)' \
  "$HOME/Library/Application Support/LocalVoiceRelay/runtime/.venv/bin/python" scripts/smoke-preference.py
/usr/bin/sandbox-exec -p '(version 1) (allow default) (deny network*)' \
  "$HOME/Library/Application Support/LocalVoiceRelay/runtime/.venv/bin/python" scripts/smoke-sentences.py
/usr/bin/sandbox-exec -p '(version 1) (allow default) (deny network*)' \
  "$HOME/Library/Application Support/LocalVoiceRelay/runtime/.venv/bin/python" scripts/smoke-lines.py
/usr/bin/sandbox-exec -p '(version 1) (allow default) (deny network*)' \
  "$HOME/Library/Application Support/LocalVoiceRelay/runtime/.venv/bin/python" scripts/smoke-lines.py --saved-reference --flatten
python3 scripts/audit-public.py
```

スモーク検証の合成音声とJSONレポートはGit対象外の `.build/smoke/`、`.build/streaming/`、`.build/preference/`、`.build/sentences/` に生成します。実ユーザの録音をこの場所に保存するワークフローではありません。

## 2026-09-09 ビルド13: 報告スキル・生成アイコン・リファクタリング

リファクタリング前にリポジトリ専用のIssue報告スキルと環境情報収集スクリプト、GitHub Issueフォームを追加し、built-in imagegenで生成したアイコンをPNG・ICNSとして保存してバンドルへ設定した。生成プロンプトは `Resources/AppIcon.md` に記録した。

変更では、設定移行をRelayCoreへ、画面の案内をAppModelPresentationへ分離した。設定・診断ログのファイル処理を共通化し、一時書込から0600を適用して原子的に置換する。workerの応答を4 KB単位で読み、改行のない途中応答と上限超過を拒否する。workerと標準読み上げに渡す環境変数を許可リスト化し、依存ライブラリの例外本文を返さない。停止後の古い認証結果の採用を防ぎ、設定保存失敗時の参照音声の後始末と、処理中の操作ガードも改善した。

検証結果:

- `scripts/test.sh`: Swift 79件・10 suites、Python worker 34件、報告／監査スクリプト3件、計116件が通過。Swiftテストには実際の開いたパイプからの読込、途中・過大応答、保存権限、シンボリックリンク拒否、報告の情報限定を含む。
- `scripts/render-ui.sh`: サンプルによる設定画面、明暗のホーム、コンパクトなログ、報告シート、サイドバーの生成アイコンを描画。新規の報告シート・サイドバーと狭い幅のログを画像で目視確認。
- `scripts/build.sh`: releaseビルド・既存証明書での署名検証・ビルド13の配置が成功。バンドルのInfo.plist・ICNS・workerがソースと一致。
- `scripts/check-offline.sh`: OS sandboxがネットワーク接続を拒否。
- 本番の `LocalWorker`・`OfflineProcess`・`WorkerResponseReader` を使う一時的な検証用バンドルで、2回のping、ローカルモデル診断、Mac標準音声で合成した一般的な日本語の文字起こしが成功。実マイク・個人の参照録音・Webex実送信は使っていない。
- Issue報告スキルと既存サポートスキルをskill-creatorのvalidatorで検証し、両方が通過。
- 公開前監査が通過。録画編集成果物をGit対象から除外し、公開するバイナリは生成アイコン2点だけをハッシュで承認。元のローカル成果物は保持。

最初の制限付き実行では音声・Visionフレームワークを初期化できなかったため、macOSの通常環境で基準テストと最終テストを実行した。既存のキーチェーン互換APIにはdeprecation警告が残る。再認証や権限を壊すAPI移行は今回行っていない。長時間待受、ロック中の実マイク、実Webex往復、本人の声の聴感、VoiceOverの全操作は今回再検証していない。

mainへの最初のpush後、GitHub Actionsでも依存取得・全テスト・公開前監査・パッケージ作成が成功した。CIで出たNode 20の廃止警告に対応し、公式リリースのcheckout v7.0.1とsetup-uv v10.0.1（いずれもNode 24）をコミットSHAで固定した。
